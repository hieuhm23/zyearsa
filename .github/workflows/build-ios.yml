name: Build iOS IPA

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: EAS Build IPA
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: npm

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.TOKE }}

      - name: 📦 Install dependencies
        run: npm install

      - name: 🚀 Build iOS App
        # Bỏ --no-wait để chờ build xong và lấy link
        # Thêm --json để dễ parse nếu cần, hoặc để mặc định để xem log
        run: eas build --platform ios --profile production --non-interactive
