name: Build iOS IPA

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: EAS Build IPA
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: npm

      # Bỏ Setup EAS của Expo action để tự cài tay cho chắc
      # - name: 🏗 Setup EAS
      #   uses: expo/expo-github-action@v8
      #   with:
      #     eas-version: latest
      #     token: ${{ secrets.TOKE }}

      - name: 📦 Install dependencies & EAS CLI
        run: |
          npm install
          npm install -g eas-cli

      - name: �️ Check Token (Debug)
        env:
          MY_TOKEN: ${{ secrets.TOKEN }}
        run: |
          if [ -z "$MY_TOKEN" ]; then
            echo "❌ LỖI RỒI: Không tìm thấy Secret TOKEN. Bạn nhớ thêm vào mục 'Secrets' nhé!"
            exit 1
          else
            echo "✅ Đã tìm thấy Secret TOKEN (Độ dài: ${#MY_TOKEN} ký tự)."
          fi

      - name: 🚀 Build iOS App
        env:
          EXPO_TOKEN: ${{ secrets.TOKEN }}
        run: eas build --platform ios --profile production --non-interactive
